// the function would take a key and a value and store the key accourding to the value
// if there is collusion, the function should know how to handle it using Separate Chaining(create an array that would contain the collision)
// the hash would be static, allocate memory to it accourding to the load factor (0.7)

